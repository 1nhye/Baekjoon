# 두 구간이 겹치는 조건

## 공식
구간 [ws, we]와 [s, e]가 겹친다 ↔

```
ws <= e and s <= we
```

## 원리 (드모르간)
겹치지 않는 조건: `we < s or e < ws`
→ 부정하면: `ws <= e and s <= we`

## 예시
```python
def overlaps(ws, we, s, e):
    return ws <= e and s <= we
```
